require "rake/testtask"

desc "Run tests."
Rake::TestTask.new :test do |t|
  t.test_files = FileList["test/**/*_test.rb"]
  t.warning = false
end

task :acceptance do
  puts "cancelled until can be run concurrently"
end
